<div class="wrapper wrapper-content project-manager">
    <h4>Inscriptions</h4>
    {% if availableSpots > 0 and tournament.tournamentDate > now %}
        {% if app.user.teams is not empty %}
            <p>
                Il reste encore des places pour vous inscrire. Veuillez sélectionner ci-dessous l'équipe
                avec laquelle vous souhaitez effectuer ce tournoi.
            </p>
            {{ form_start(form) }}
            {{ form_widget(form.teams) }}
            <div class="d-flex justify-content-center">
                <button class="btn btn-primary my-3">{{ button_label|default('S\'inscrire') }}</button>
            </div>
            {{ form_end(form) }}
            <p class="small fw-bold">Si vous souhaitez vous inscrire avec une autre équipe, vous pouvez
                en
                créer une <a
                        href="{{ path('app_team_new') }}">ici</a> ou réactiver une ancienne équipe sur
                votre
                profil.</p>
        {% else %}
            <p>
                Avant de pouvoir vous inscrire, vous devez d'abord créer une équipe <a
                        href="{{ path('app_team_new') }}" class="fw-bold">ici</a>.
            </p>
        {% endif %}
    {% else %}
        <p>Les inscriptions sont fermées !</p>
    {% endif %}
</div>